import type{VercelRequest,VercelResponse}from"@vercel/node";export default async function handler(req:VercelRequest,res:VercelResponse){if(req.method!=="POST")return res.status(405).json({error:"Method not allowed"});const{name,email,interest,message,property}=req.body||{};if(!name||!email)return res.status(400).json({error:"Name and email are required"});const key=process.env.RESEND_API_KEY;if(!key)return res.status(200).json({ok:true,demo:true,message:"Inquiry accepted in demo mode"});const to=process.env.CONTACT_TO_EMAIL;if(!to)return res.status(500).json({error:"CONTACT_TO_EMAIL is not configured"});const response=await fetch("https://api.resend.com/emails",{method:"POST",headers:{"Authorization":`Bearer ${key}`,"Content-Type":"application/json"},body:JSON.stringify({from:process.env.CONTACT_FROM_EMAIL||"Northline <onboarding@resend.dev>",to:[to],subject:`Northline inquiry — ${name}`,html:`<h2>New Northline inquiry</h2><p><b>Name:</b> ${name}</p><p><b>Email:</b> ${email}</p><p><b>Interest:</b> ${interest||"—"}</p><p><b>Property:</b> ${property||"—"}</p><p><b>Message:</b> ${message||"—"}</p>`})});if(!response.ok)return res.status(502).json({error:"Email provider rejected the request"});return res.status(200).json({ok:true})}